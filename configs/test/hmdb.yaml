INFO: 'test_3_notv_sem-1mlp_task2'
DEBUG: true
MODEL: 
  NAME: 'test'
  BACKBONE: 'RN50'          # ["RN50", "ViT-B/16"]
  SEED: 6
  USE_CLASSIFICATION: true
  USE_CLASSIFICATION_VALUE: 1.5
  MOTION_COFF: 0.
  NORMAL_COFF: 1.
  LMD: [0.5, 1, 0] # logits = lambdas[1] * target_logits + lambdas[2] * target_logits_global 
          # loss =  lambdas[0] * l_class_logits + lambdas[1] * l_target_logits + lambdas[2] * l_target_logits_global

TRAIN:
  WAY: 5
  SHOT: 1
  QUERY_PER_CLASS: 3

  TRAIN_EPISODES: 20010
  VAL_FREQ: 1000
  CLASS_NAME: ['brush hair', 'catch', 'chew', 'clap', 'climb', 'climb stairs', 'dive', 'draw_sword', 'dribble', 'drink', 'fall floor', 'flic flac', 'handstand', 'hug', 'jump', 'kiss', 'pullup', 'punch', 'push', 'ride_bike', 'ride horse', 'shake_hands', 'shoot_bow', 'situp', 'stand', 'sword', 'sword exercise', 'throw', 'turn', 'walk', 'wave']
  
TEST:
  ONLY_TEST: false
  QUERY_PER_CLASS: 1
  TEST_EPISODES: 5000
  TEST_MODEL_PATH: 'checkpoint/hmdb/1shot/test/checkpoint_best.pt'
  CLASS_NAME: ['fencing', 'kick', 'kick ball', 'pick', 'pour', 'pushup', 'run', 'sit', 'smoke', 'talk']

DATA:
  DATASET: 'hmdb'   # ["ssv2", 'ssv2_cmn', "kinetics", "hmdb", "ucf"]
  DATA_DIR: '/home/zhangbin/tx/datasets/'
  SEQ_LEN: 8
  IMG_SIZE: 224
  NUM_WORKERS: 8

CHECKPOINT:
  CHECKPOINT_DIR: 'checkpoint/hmdb/1shot/test'
  SAVE_FREQ: 20000

SOLVER:
  LR: 0.00001
  WEIGHT_DECAY: 5e-5
  OPTIM_METHOD: 'adam'   # ["adam", "sgd"]

